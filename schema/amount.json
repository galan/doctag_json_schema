{ "type":"object",
  "title": "Amount",
  "name": "amount",
  "description": "Gross and net total values for a total",
  "properties":{
    "gross":{
      "description": "Gross total",
      "type":"number",
      "required": true
    },
    "net":{
      "description": "Net total",
      "type":"number",
      "required": true
    }
  }
}